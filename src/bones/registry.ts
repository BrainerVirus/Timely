/**
 * Boneyard skeleton registry. Regenerate with the dev server running, e.g.:
 * `npx boneyard-js build http://127.0.0.1:1420` (visit issue hub so `issue-hub-page` captures).
 * The Vite plugin uses `skipInitial: true` so `vite build` does not launch Playwright.
 */
import { registerBones } from "boneyard-js/react";

registerBones({});
